using UnityEngine;
using System.Collections;

public class CamelVisionScript : MonoBehaviour
{

    /*private SphereCollider visionSphere;
    private CamelScript camelScript;

	void Start()
    {
        visionSphere = gameObject.GetComponent<SphereCollider>();
        camelScript = transform.parent.GetComponent<CamelScript>();
	}

    void OnTriggerStay(Collider other)
    {
        if (other.tag == "Enemy")
        {
            camelScript.AcknowledgeEnemy();
        }
    }*/
}
